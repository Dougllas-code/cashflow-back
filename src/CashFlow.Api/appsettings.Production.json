{
  "ConnectionStrings": {
    "Connection": "Server=mysql;Database=${MYSQL_DATABASE};Uid=root;Pwd=${MYSQL_ROOT_PASSWORD};"
  },
  "Settings": {
    "Jwt": {
      "SigningKey": "${JWT_SIGNING_KEY}",
      "ExpiresMinutes": 60
    },
    "RabbitMq": {
      "Host": "rabbitmq",
      "Username": "${RABBITMQ_DEFAULT_USER}",
      "Password": "${RABBITMQ_DEFAULT_PASS}"
    }
  }
}